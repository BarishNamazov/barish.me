---
import { formatDate, getDatetime, type DateValue } from "../utils/dateFormatting";

interface Props {
  date: DateValue;
  format?: "short" | "long";
}

const { date, format = "short" } = Astro.props;
---

<time datetime={getDatetime(date)}>
  {formatDate(date, format)}
</time>
